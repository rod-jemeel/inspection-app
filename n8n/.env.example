# n8n Basic Auth Credentials
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme

# n8n Webhook Configuration
# For production, set this to your public n8n URL (e.g., https://n8n.yourdomain.com)
# For local development, use http://localhost:5678
N8N_WEBHOOK_URL=http://localhost:5678

# Shared Secret for HMAC Signature Verification
# IMPORTANT: Must match N8N_WEBHOOK_SECRET in your Next.js app's .env
# Generate a secure random string (e.g., using: openssl rand -hex 32)
N8N_WEBHOOK_SECRET=your-shared-secret-here

# Next.js Application Base URL
# For local development: http://host.docker.internal:3000 (allows Docker to reach host)
# For production: https://app.yourdomain.com
APP_BASE_URL=http://host.docker.internal:3000
